# Data Models — LeadGen

> Generated by bmad-document-project (deep scan) on 2026-05-29.

## Status

There is **no database, ORM, schema, or migration in the codebase**. The root `package.json` declares a `db:init` script (`tsx database/scripts/init.ts`) and `pg`/`dotenv`/`tsx` dependencies, plus `DATABASE_URL` in `.env.example`, but the `database/` package does not exist yet, so the script cannot run.

The only data in the running app is **hardcoded mock data** inside the two view components. The planned production schema is specified (but not implemented) in [../specs/data-model.md](../specs/data-model.md).

## Implemented mock data shapes (inferred from UI code)

### Lead (`leads[]` in lead-generation-view.tsx)

```ts
type Lead = {
  id: string;
  company: string;
  location: string;
  region: string;          // "US" | "DACH" | "UK" | "EU" (filter dimension)
  segment: string;
  vertical: string;        // filter dimension
  companySize: string;     // "50-500" | "500-5000"
  buyerFocus: string;      // "COO" | "VP Ops" | "VP Engineering" | "CTO"
  score: number;           // fit score 0-100
  urgency: "High" | "Medium" | "Low";
  status: string;          // "New" | "Enriched" | "CRM ready" | "Watch later"
  notifications: { email: boolean; webhook: boolean };
  summary: string;
  happened: string;        // "What Happened" blurb
  signals: string[];
  contacts: { name: string; title: string; channel: string }[];
  evidence: { source: string; time: string; text: string }[];
};
```

### Account (`accounts[]` in monitoring-view.tsx)

```ts
type Account = {
  id: string;
  name: string;
  tier: string;            // "Strategic" | "Expansion" | "Nurture"
  urgency: "High" | "Medium" | "Low";
  score: number;           // signal strength 0-100
  sources: number;         // count
  latest: string;          // human-readable recency, e.g. "18 min ago"
  notifications: { email: boolean; webhook: boolean };
  summary: string;         // "Last 3 Hours" blurb
  actions: string[];       // next best actions
  signals: { type: string; time: string; title: string; evidence: string; urgency: string }[];
  sourcesList: string[];   // custom source labels
};
```

### Shared UI types

- `ViewType = "lead-generation" | "monitoring"` (`app/src/app/types.ts`)
- `NavBarItem = { title: string; id: ViewType; icon: LucideIcon }` (`nav-main.tsx`)

> These mock shapes are a UI convenience, not the data contract. They overlap conceptually with the planned `lead_candidates`, `accounts`, `signals`, `signal_evidence`, and `contacts` tables but are flattened and denormalized for rendering.

## Planned production schema (not implemented)

See [../specs/data-model.md](../specs/data-model.md) for the full target schema, including:

- Core tables: `accounts`, `watchlists`, `icp_rules`, `lead_candidates`, `sources`, `artifacts`, `signals`, `signal_evidence`, `contacts`, `alerts`, `suppression_entries`, `audit_log`, etc.
- Required evidence fields, per-source-type dedupe keys, signal types, scoring with decay, and compliance objects.

When the `database/` package is built, regenerate this document (deep scan) to replace the mock shapes with the real schema.
