# Source Tree Analysis — LeadGen

> Generated by bmad-document-project (deep scan) on 2026-05-29.

Annotated tree of what **exists in the repository**. Folders named in the README but absent on disk (`database/`, `services/`) are flagged.

```text
leadgen/
├── app/                          # Next.js dashboard — the ONLY implemented part
│   ├── package.json              # app deps: next 15.3.8, react 19, clerk, radix, sonner, tailwind 4
│   ├── next.config.ts            # empty Next config (defaults)
│   ├── postcss.config.mjs        # Tailwind v4 PostCSS plugin
│   ├── tsconfig.json             # path alias "@/*" → src/*
│   └── src/
│       ├── app/                  # App Router root (single route)
│       │   ├── layout.tsx        # ⮕ ENTRY: root layout, metadata, conditional ClerkProvider, Toaster
│       │   ├── page.tsx          # ⮕ ENTRY: DashboardPage — view switch (lead-generation | monitoring)
│       │   ├── globals.css       # Tailwind layer + brand CSS variables
│       │   ├── types.ts          # ViewType union
│       │   ├── components/       # App-specific layout components
│       │   │   ├── app-sidebar.tsx   # Sidebar shell + nav item config
│       │   │   ├── nav-main.tsx      # Workspace nav (view switcher buttons)
│       │   │   └── nav-user.tsx      # User dropdown (static fallbackUser)
│       │   └── views/            # The two product pillars (contain mock data)
│       │       ├── lead-generation-view.tsx  # `leads[]` mock + discovery inbox + ICP/Filters modals
│       │       └── monitoring-view.tsx       # `accounts[]` mock + watchlist + account hub + Add Account modal
│       ├── components/ui/        # shadcn/ui primitives (Radix-based)
│       │   ├── avatar.tsx  badge.tsx  button.tsx  card.tsx
│       │   ├── dropdown-menu.tsx  input.tsx  progress.tsx
│       │   ├── separator.tsx  sidebar.tsx  sonner.tsx  textarea.tsx
│       └── lib/
│           ├── auth-mode.ts      # Clerk dev-bypass toggle via NEXT_PUBLIC_AUTH_MODE
│           └── utils.ts          # cn() — clsx + tailwind-merge
│
├── specs/                        # Product/architecture source docs (Markdown)
│   ├── product.md  architecture.md  data-model.md
│   ├── sources.md  phases.md  ui-mapping.md
│   ├── LeadGen Monitoring feature.md             # Jira-ready monitoring backlog
│   └── Monitoring_Feature_Jira_Ready_Backlog.docx
│
├── _bmad-output/specs/spec-leadgen/   # Distilled SPEC kernel (bmad-spec output)
│   ├── SPEC.md  .decision-log.md
│
├── docs/                         # ⮕ THIS documentation set (project_knowledge)
│
├── package.json                  # root: workspaces=["app"], db:init script (target missing)
├── package-lock.json
├── tsconfig.json                 # root TS config
├── .env.example                  # full env surface (DB, AWS, Pinecone, Cohere, Nebius, crawlers, Slack/Telegram)
├── README.md
└── .gitignore

# Referenced by README/scripts but NOT present in the repo:
#   database/            (db:init → database/scripts/init.ts would fail)
#   services/ingest/     (Go polling/normalization service)
#   services/worker/     (Go deep-processing service)
```

## Critical folders

| Path | Role |
| --- | --- |
| `app/src/app/` | App Router root; the single page and layout live here |
| `app/src/app/views/` | The two product pillars and their mock datasets — the heart of the prototype |
| `app/src/app/components/` | App-level layout (sidebar, nav, user menu) |
| `app/src/components/ui/` | Reusable shadcn/ui primitives |
| `app/src/lib/` | Cross-cutting helpers (`cn`, auth-mode) |
| `specs/` | Authoritative product & architecture intent |
| `_bmad-output/specs/spec-leadgen/` | Distilled, validated SPEC contract |

## Entry points

- **`app/src/app/layout.tsx`** — wraps the app, conditionally mounts Clerk, renders the global toaster.
- **`app/src/app/page.tsx`** — the dashboard; owns the `selectedView` state that toggles between the two views.

## Path aliases

`@/*` → `app/src/*` (configured in `app/tsconfig.json`). Imports like `@/components/ui/button` and `@/app/views/...` resolve through this alias.
