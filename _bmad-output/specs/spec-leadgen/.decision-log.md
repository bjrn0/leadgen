# Decision Log — SPEC-leadgen

## 2026-05-29 — Initial create (distill all of `specs/` → product-level SPEC)

**Inputs absorbed:** `specs/product.md` (→ kernel, listed as source), `specs/architecture.md`, `data-model.md`, `sources.md`, `ui-mapping.md`, `phases.md`, `LeadGen Monitoring feature.md` (→ adopted companions). The `.docx` twin of the monitoring backlog was not read (binary); its `.md` equivalent covers the same content.

**Routing decisions:**
- Kernel lifted 11 capabilities, constraints, non-goals, and success signal from `product.md`'s vision/principles/success criteria.
- Hot-path/cold-path was routed to **Constraints**, not a capability — it is a HOW (mechanism), per Spec Law rule 2.
- The rich authored docs were **adopted** as companions (referenced by relative path, not edited or duplicated into the kernel) because they hold load-bearing catalogs/tables/diagrams the data model, source strategy, UI→event mappings, AWS architecture and downstream must read. Diagrams already live in `architecture.md`, satisfying the "diagrams always in a companion" rule.
- `product.md` listed under `sources:` — its load-bearing content is now in the kernel; downstream does not need to re-read it.

**Pass 1 — Coherence (Spec Law 1–6, 8):** PASS. All 11 capabilities carry both `intent` and `success` (rule 1); intents describe WHAT (rule 2); each constraint rules something out (rule 3); four non-goals present (rule 4); the success signal is demonstrable (rule 5); capability IDs CAP-1..CAP-11 are unique and stable (rule 6); prose is lean (rule 8). Three inferred reconciliations between the broad vision and the concrete backlog were not directly confirmed, so they are logged as `Assumptions`.

**Pass 2 — Preservation (rule 7):** PASS with documented divergences. Every load-bearing source claim lands in the kernel or an adopted companion. Genuine conflicts between `product.md` and the monitoring backlog (CRM dependency, backlog-only features, cadence, person identity) were not silently resolved — they are recorded as `Open Questions` for a human decision before downstream consumes the spec. No wrapper-only content was dropped (the inputs were near-pure load-bearing product content).

**Verdict:** Coherent and preservation-complete as a create. The spec is consumable now, but four Open Questions (chiefly the CRM-scope conflict between the product vision and the V1 monitoring backlog) should be resolved before architecture/PRD work hardens around them.

## 2026-05-29 — Update (Open Questions resolved by user)

All four Open Questions answered, folded into the kernel, and the Open Questions section removed:

1. **CRM in V1 → Yes.** CRM *activation* (one-way push + note/opportunity logging) is in V1 via CAP-9. Refined the non-goal to scope out only *bidirectional/full* CRM sync and Apollo/API integrations.
2. **Backlog-only features in V1 → Yes (whole product).** Added CAP-12 (grouping/tagging), CAP-13 (follow-up reminders, no CRM dependency), CAP-14 (ownership/claim + activity log), CAP-15 (one-way CSV export). The first assumption was rewritten to record this as confirmed scope.
3. **Cadence → per-account/per-source configurable.** Folded into CAP-2 success (default 3-hour digest, configurable per account and per source). Cadence assumption/question retired.
4. **Person identity → one unified Person entity.** Promoted to a Constraint ("One Person entity" across both pillars). Person assumption/question retired.

**Re-validate:** Pass 1 — capabilities now CAP-1..CAP-15, each with intent+success, IDs stable (no renumber). Pass 2 — no source claims dropped; all four resolutions trace to user direction in this conversation. Open Questions section now empty and omitted. Spec is consumable for PRD/architecture.

**Source update:** user chose SPEC-only — resolutions are NOT mirrored back into `specs/`. The originating files retain their CRM-scope wording conflict; SPEC.md is the authoritative reconciliation. Downstream consumes SPEC.md + companions, treating `product.md` as traceability-only per its `sources:` listing.
